{{- if .Values.letsencrypt.email }}
apiVersion: batch/v1
kind: Job
metadata:
  name: delete-default-issuers
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": pre-delete
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    metadata:
      name: delete-default-issuers
      namespace: {{ .Release.Namespace }}
    spec:
      restartPolicy: Never
      containers:
      - name: delete-default-issuers
        image: gcr.io/google_containers/hyperkube-amd64:v1.9.4
        command: ["kubectl","delete","-f","/issuers/"]
        volumeMounts:
          - name: issuers
            mountPath: /issuers
      volumes:
        - name: issuers
          configMap:
            name: issuers
      serviceAccountName: {{ template "cert-manager.serviceAccountName" . }}-hook
{{- end }}
