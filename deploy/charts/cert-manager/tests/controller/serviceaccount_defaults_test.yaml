suite: Controller ServiceAccount - Default Rendering
templates:
  - templates/serviceaccount.yaml
release:
  name: cert-manager
  namespace: cert-manager
tests:
  - it: should render a ServiceAccount matching the default snapshot
    asserts:
      - isKind:
          of: ServiceAccount
      - matchSnapshot: {}
